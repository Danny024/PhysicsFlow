<UserControl x:Class="PhysicsFlow.App.Views.AIAssistant.ChatMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PhysicsFlow.ViewModels;assembly=PhysicsFlow.ViewModels">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <!-- User message: right-aligned blue bubble; Assistant: left-aligned dark bubble -->
    <Grid>
        <!-- User bubble -->
        <Border Visibility="{Binding IsUser, Converter={StaticResource BoolToVis}}"
                HorizontalAlignment="Right"
                MaxWidth="480"
                Margin="48,0,0,0"
                Background="#1565C0"
                CornerRadius="12,12,2,12"
                Padding="12,8">
            <TextBlock Text="{Binding Content}"
                       Foreground="White"
                       FontSize="13"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Assistant bubble -->
        <Border Visibility="{Binding IsAssistant, Converter={StaticResource BoolToVis}}"
                HorizontalAlignment="Left"
                MaxWidth="560"
                Margin="0,0,48,0"
                Background="#2A2A3E"
                CornerRadius="12,12,12,2"
                Padding="12,8">
            <StackPanel>
                <TextBlock Text="{Binding Content}"
                           Foreground="#E8E8F0"
                           FontSize="13"
                           TextWrapping="Wrap"/>

                <!-- Tool call indicator -->
                <TextBlock Text="🔧 Tool called"
                           Foreground="#00B0D8"
                           FontSize="10"
                           Margin="0,4,0,0"
                           Visibility="{Binding HasToolCalls, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
